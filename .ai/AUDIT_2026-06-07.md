# Audit 2026-06-07

## Overall status

- Delivery state: AUTOMATED RELEASE GATES PASSED; MANUAL DPI ACCEPTANCE PENDING.
- Scope: multi-agent review of UI wiring, data integrity, security, architecture,
  tests, packaging contracts, and handoff/spec drift.
- Full-suite verification: `python -m pytest -q` => 1108 passed, exit code 0,
  in 762.02 seconds.
- Post-test hygiene: no project pytest/TaxOps/PyInstaller process and no project
  `.part` or `.restore_stage_*` artifact remained.
- Fresh-environment packaging, EXE smoke, ZIP readback, and SHA-256: VERIFIED.
- Git push, tag, and GitHub Release: included in the v0.25.0 closure.

## Verified fixes

| Area | Evidence | Status |
|---|---|---|
| Stable UI selection | Client and engagement refresh restore selection by stable database ID | VERIFIED |
| Rich-text injection | Dynamic document-request, recurring-billing, and bulk-import labels force plain text | VERIFIED |
| Backup integrity | WAL-aware snapshot, exact schema checks, unknown object rejection, foreign-key/integrity checks, closed temporary connections | VERIFIED |
| Backup isolation | Development and production use separate backup directories; filenames include microseconds | VERIFIED |
| Image resources | File/dimension/pixel limits for workflow and canvas imports | VERIFIED |
| Workflow image lifecycle | Reference-aware cleanup; corrupt historical JSON cannot cause post-commit false failure or unsafe deletion | VERIFIED |
| Registry import | ZIP/member/ratio/row/field limits and atomic cache/metadata/audit transaction | VERIFIED |
| Registry download | Requested and final redirect URLs must remain on the HTTPS official-domain allowlist | VERIFIED |
| Deleted-client isolation | Late-fee, workflow, and canvas reads exclude inactive owners | VERIFIED |
| Task bulk operations | Bulk create/delete and audit writes are one transaction with rollback tests | VERIFIED |
| Recurring billing | Public occurrence-generation return contract restored; load failures show error state instead of fake empty data | VERIFIED |
| Layout safety | FlowLayout teardown guard and screen-aware main-window minimum size | VERIFIED |
| Packaging contract | PyInstaller dynamically collects all migration modules | VERIFIED |
| Single instance | Real cross-process activation test replaces the skipped mock-only test | VERIFIED |
| Attachment preview | Shared decoded-pixel guard runs before `QPixmap` decoding | VERIFIED |
| Registry worker | Real QThread success/error/connection-close tests; app close blocked while active | VERIFIED |
| Action registry | Every enabled handler string resolves to a callable; markers are documented scenario IDs | VERIFIED |
| Release dependencies | Exact Windows release pins; project-scoped audit reports 0 known vulnerabilities | VERIFIED |
| EXE package | Fresh venv build, isolated SQLite smoke, resource hygiene, ZIP readback, SHA-256 | VERIFIED |

## Residual risks

| Risk | Status | Required next evidence |
|---|---|---|
| Broad persistence tests reuse the same SQLite connection | DEFERRED_WITH_RISK | Cross-connection readback campaign for critical writes |
| Audit actor identity and tamper evidence are weak | DEFERRED_WITH_RISK | Stable operator identity plus chained/hash or protected audit storage |
| `ServiceContainer` exposes raw connection and services use repository `_conn` (18 static matches) | DEFERRED_WITH_RISK | Transaction boundary ADR and incremental public transaction API |
| Several UI pages remain large and repeat exception/state handling (`document_requests_page.py` 1209 lines; `work_records_page.py` 1201 lines) | DEFERRED_WITH_RISK | Characterization tests followed by scoped extraction |
| SBOM and license baseline are absent | DEFERRED_WITH_RISK | Add CI-generated SBOM and license scan |
| Real EXE at 1366x768 and 100/125/150/200 percent DPI | UNKNOWN | Manual/computer-use acceptance with screenshots |

## Security scan triage

- Bandit output: `.ai/bandit-latest.json`.
- B701: Jinja autoescape warning is not an HTML-rendering boundary. Message templates
  are plain text and enforce an AST allowlist that rejects attributes, filters,
  calls, expressions, and control flow, including direct-SQL injected templates.
- B608: dynamic SQL findings are primarily fixed table names or allowlisted sort
  columns with bound data values. They remain scan noise unless a non-allowlisted
  identifier or data value is found.
- B310: actionable redirect issue was fixed by validating the final response URL
  before reading the response body.
- Release dependency output: `.ai/pip-audit-release-v0.25.0.json`.
- The exact release set contains 15 dependencies and reports 0 known
  vulnerabilities.
- Local-environment dependency output: `.ai/pip-audit-latest.json`.
- The local scan reports 64 advisories across 22 of 297 installed packages, but
  most affected packages are not TaxOps dependencies. Direct runtime packages
  PySide6 6.10.2, Jinja2 3.1.6, and openpyxl 3.1.5 were not reported. The
  development environment's pytest 8.3.3 was reported. This is not a release
  SBOM because the project has no lockfile and the machine environment is shared.

## Release gate

Do not call this release deliverable until:

1. The final full pytest run passes on the final worktree. VERIFIED.
2. EXE packaging and smoke testing pass. VERIFIED.
3. ZIP readback and SHA-256 pass. VERIFIED.
4. Manual/computer-use DPI acceptance is recorded. PENDING because the
   Computer Use connector failed to initialize.
5. Residual architecture/audit risks remain documented and are not hidden by
   the release result.
